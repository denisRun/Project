@model Dictionary<string, decimal>
@{
    ViewBag.Title = "MoneyPerMonth";
}

<h2>Money Per Month</h2>

<table class="table table-hover">
    <thead>
        <tr>
            <th>
                @Html.Label("Month")
            </th>
            <th>
                @Html.Label("Money")
            </th>
        </tr>
    </thead>
    <tbody>
        @foreach (var value in Model)
        {
            <tr>
                <td>
                    @Html.Raw(value.Key)
                </td>
                <td>
                    @Html.Raw(value.Value)
                </td>           
            </tr>
}
    </tbody>
</table>
